@{
    ViewData["Title"] = Localizer["Access Denied"];
}

<div class="text-center">
    <h1 class="display-5 text-danger">@Localizer["Access Denied"]</h1>
    <p class="lead">@Localizer["You do not have permission to view this page."]</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">@Localizer["Back to Home"]</a>
</div>
