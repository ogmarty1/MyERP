@model ERP.Web.Models.CategoryFormViewModel

@{
    ViewData["Title"] = Localizer["Edit Category"];
}

<h1>@Localizer["Edit Category"]</h1>

<form asp-action="Edit" asp-route-id="@Model.Id" method="post" class="js-submit-loading">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    @await Html.PartialAsync("_CategoryForm", Model)
</form>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
